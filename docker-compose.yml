services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: getway
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - getway_pg:/var/lib/postgresql/data
volumes:
  getway_pg:
