import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import rateLimit from 'express-rate-limit'; import { env } from './config.js'; import { auth } from './middleware/auth.js';
import authRoutes from './routes/auth.js'; import subscriptionRoutes from './routes/subscriptions.js'; import paymentLinkRoutes from './routes/payment-links.js'; import transferRoutes from './routes/transfers.js'; import customerRoutes from './routes/customers.js'; import paymentRoutes from './routes/payments.js'; import dashboardRoutes from './routes/dashboard.js'; import webhookRoutes from './routes/webhooks.js'; import adminRoutes from './routes/admin.js';
const app=express();app.use(helmet());app.use('/api',rateLimit({windowMs:60_000,limit:180,standardHeaders:'draft-7',legacyHeaders:false}));app.use(cors({origin:env.CORS_ORIGIN.split(',').map(s=>s.trim()),credentials:true}));app.use(express.json({limit:'1mb'}));
app.get('/health',(_,res)=>res.json({status:'ok',provider:env.PAYMENT_PROVIDER}));app.use('/api/auth',authRoutes);app.use('/webhooks',webhookRoutes);app.use('/api/dashboard',auth,dashboardRoutes);app.use('/api/customers',auth,customerRoutes);app.use('/api/payments',auth,paymentRoutes);app.use('/api/subscriptions',auth,subscriptionRoutes);app.use('/api/payment-links',auth,paymentLinkRoutes);app.use('/api/transfers',auth,transferRoutes);app.use('/api/admin',auth,adminRoutes);
app.use((_,res)=>res.status(404).json({error:'Rota não encontrada'}));
app.listen(env.PORT,()=>console.log(`Getway API http://localhost:${env.PORT}`));
